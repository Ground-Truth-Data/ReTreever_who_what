node_modules/
.svelte-kit/
.DS_Store
*.log

# ── Environment variables & secrets ─────────────────────────────────────────
# This repo is PUBLIC and is also packed into the npm tarball, and npm-packlist
# honours this file. Mirrors ReTreever/.gitignore:89-106.
.env
.env.*
.env.local
!.env.example
credentials*.json
*.pem
*.key
secrets/
.secrets/
# Cloudflare Workers local secrets + state.
.dev.vars
.wrangler/

# standaloneTest.sh vendors siblings in here to prove this child runs
# with no workspace around it, then leaves the copy behind. Generated, never
# committed — unlike _rapper/, which IS tracked so a lone clone can run.
_siblings/
